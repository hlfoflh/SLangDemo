# Asset

## 目录职责

本目录用于存放未来 demo 和实验需要的美术资源、测试场景、模型、纹理、reference 图像和小型输入数据。

## 初始约定

- 优先放小型、可复现、许可证清晰的资源。
- 大型数据集或生成结果不放在这里，应放入外部数据目录或 `compiled-out/`。
- 资源命名应体现用途，例如 `basic_triangle/`、`material_fit/`、`gradient_check/`。


